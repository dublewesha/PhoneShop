@model Phone
@{
    var photoPath = $"/img/{Model.PhotoPath}";
    var photoPath1 = $"/img/{Model.PhotoPath1}";
    var photoPath2 = $"/img/{Model.PhotoPath2}";
    var name = Model.Name;
    var price = Model.Price;
}


